default:
  formatters:
    progress: true
  suites:
    orm:
      paths: ["%paths.base%/features/example-app"]
      contexts:
        - Context\ExampleAppContext

  autoload:
    'Context': "%paths.base%/features"
